/**================================================================================================
 									custom filter by data										  
===================================================================================================
 @Version: 1.0
 @Author: 
 @LastUpdate:
===================================================================================================
=================================================================================================*/
eApp.filter('padLeft', function() {
    return function (value, count, char) {
//    	console.log('value=' + value + ', count=' + count + ', char='
//				+ char);

		while (value.length < count) {
			value = char + value;
		}
		return value;
    }
});